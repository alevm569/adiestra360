asgiref==3.11.1
Django==5.2.14
django-cors-headers==4.9.0
djangorestframework==3.17.1
djangorestframework_simplejwt==5.5.1
# MySQL solo en local (Windows). En la nube (Linux) se usa PostgreSQL, así el
# build de Render no intenta compilar mysqlclient (que necesita libs de sistema).
mysqlclient==2.2.8; platform_system == "Windows"
packaging==26.0
PyJWT==2.13.0
python-dotenv==1.2.2
sqlparse==0.5.5
tzdata==2026.2
# --- Producción ---
gunicorn==23.0.0
whitenoise==6.9.0
# PostgreSQL (Neon) + parseo de DATABASE_URL para el despliegue en la nube.
psycopg2-binary==2.9.10
dj-database-url==2.3.0
